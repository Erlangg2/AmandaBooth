export const centerCol = {
  display: "flex",
  flexDirection: "column",
  alignItems: "center",
  gap: 20
};

export const row = {
  display: "flex",
  gap: 40
};

export const buttonStyle = {
  padding: "10px 18px",
  borderRadius: 12,
  border: "none",
  cursor: "pointer",
  fontFamily: "CantikaCute",
  background:
    "linear-gradient(135deg,#ff9fb2,#ff7aa2)",
  color: "white"
};

export const backBtn = {
  position: "absolute",
  left: 0,
  top: 0,
  padding: "6px 12px",
  borderRadius: 10,
  border: "none",
  background: "#ffd1dc",
  cursor: "pointer"
};

export const frameThumb = {
  width: 180,
  borderRadius: 16,
  cursor: "pointer",
  padding: 6,
  background: "#fff",
  transition: "all 0.25s ease",
  boxShadow:
    "0 6px 15px rgba(0,0,0,0.1)"
};

export const canvasStyle = {
  width: 220,
  height: 520,
  borderRadius: 20,
  background: "#fff",
  border: "4px solid #ffe3ea"
};

export const titleBar = {
  textAlign: "center",
  color: "#d17b88"
};

export const webcamStyle = {
  width: 380,
  height: 520,
  borderRadius: 20,
  objectFit: "cover",
  background: "#000"
};